import { take, call, put, select } from 'redux-saga/effects';
import getProject from '../getProject.saga';
import expect from 'expect';

const saga = getProject();

describe('getProject Saga', () => {

});
