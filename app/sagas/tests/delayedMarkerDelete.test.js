import { take, call, put, select } from 'redux-saga/effects';
import delayedMarkerDelete from '../delayedMarkerDelete.saga';
import expect from 'expect';

const saga = delayedMarkerDelete();

describe('delayedMarkerDelete Saga', () => {

});
