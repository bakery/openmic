import { take, call, put, select } from 'redux-saga/effects';
import uploadImage from '../uploadImage.saga';
import expect from 'expect';

const saga = uploadImage();

describe('uploadImage Saga', () => {

});
