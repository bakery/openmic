import { take, call, put, select } from 'redux-saga/effects';
import syncMarkers from '../syncMarkers.saga';
import expect from 'expect';

const saga = syncMarkers();

describe('syncMarkers Saga', () => {

});
