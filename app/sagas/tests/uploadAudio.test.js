import { take, call, put, select } from 'redux-saga/effects';
import uploadAudio from '../uploadAudio.saga';
import expect from 'expect';

const saga = uploadAudio();

describe('uploadAudio Saga', () => {

});
